import { BoostStatus } from "@/components/billing/BoostStatus";

/** P11 S5 — Boost status (seller.homzlist.com/boost). */
export const dynamic = "force-dynamic";

export default function BoostPage() {
  return <BoostStatus />;
}
