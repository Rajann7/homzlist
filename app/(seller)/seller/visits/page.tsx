import { Visits } from "@/components/listings/Visits";

/** P8 S1 — My Visits (seller.homzlist.com/visits). */
export const dynamic = "force-dynamic";

export default function VisitsPage() {
  return <Visits />;
}
