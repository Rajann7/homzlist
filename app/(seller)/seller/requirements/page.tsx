import { RequirementsBrowse } from "@/components/listings/RequirementsBrowse";

/** P8 S3 — Requirements Browse (seller.homzlist.com/requirements). */
export const dynamic = "force-dynamic";

export default function RequirementsBrowsePage() {
  return <RequirementsBrowse />;
}
