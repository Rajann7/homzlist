import { RequirementForm } from "@/components/listings/RequirementForm";

/** P6 S4 — post a requirement (seller.homzlist.com/requirements/new). */
export const dynamic = "force-dynamic";

export default function RequirementFormPage() {
  return <RequirementForm />;
}
