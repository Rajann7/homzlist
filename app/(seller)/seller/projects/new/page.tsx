import { ProjectForm } from "@/components/listings/ProjectForm";

/** P6 S5 — Builder project form (seller.homzlist.com/projects/new). */
export const dynamic = "force-dynamic";

export default function ProjectFormPage() {
  return <ProjectForm />;
}
