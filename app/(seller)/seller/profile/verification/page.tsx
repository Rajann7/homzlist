import { Verification } from "@/components/profile/Verification";

/** seller.homzlist.com/profile/verification — P9 S4. */
export const metadata = { title: "Verification" };

export default function VerificationPage() {
  return <Verification />;
}
