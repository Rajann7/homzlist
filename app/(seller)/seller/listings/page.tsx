import { MyListings } from "@/components/listings/MyListings";

/** P6 S6 — My Listings manager. */
export const dynamic = "force-dynamic";

export default function Page() {
  return <MyListings />;
}
