import { Leads } from "@/components/listings/Leads";

/** P8 S2 — Leads Pipeline (seller.homzlist.com/leads). */
export const dynamic = "force-dynamic";

export default function LeadsPage() {
  return <Leads />;
}
